# Copy this file to `secrets.toml` in the same folder (`.streamlit/secrets.toml`).
# `secrets.toml` is gitignored — never commit real keys.
#
# Used by the app so `OPENAI_API_KEY` is available to `app.fraud_report_openai`.

OPENAI_API_KEY = "sk-your-key-here"
